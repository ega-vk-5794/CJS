{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "git -C \"${CLAUDE_PROJECT_DIR:-.}\" pull --ff-only 2>&1 || true",
            "timeout": 60,
            "statusMessage": "Pulling from origin/main..."
          }
        ]
      }
    ]
  }
}
